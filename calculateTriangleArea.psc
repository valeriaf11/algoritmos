Algoritmo calculateTriangleArea
	// Define the function to calculate the area of a triangle
		// Prompts the user to enter the base and height of the triangle
		Escribir "Enter the base of the triangle: ";
		Leer base;
		Escribir "Enter the height of the triangle: ";
		Leer height;
		
		// Calculate the area of the triangle using the formula (base * height) / 2
		Area <- (Base * Height) / 2;
		
		// Show the result
		Escribir "The angle area is:", area;
FinAlgoritmo
